# Landing Comercial

Landing independiente extraida de `moisesvalero.es/diseno-web`.

## Comandos

```bash
npm install
npm run dev
npm run build
```

La ruta principal es `/diseno-web`; `/` redirige a esa landing.
